<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Synthesisproject" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="backgroundColor">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="Theme.Synthesisproject" parent="Base.Theme.Synthesisproject" />

    <style name="Widget.MyApp.CircularProgressIndicator" parent="Widget.MaterialComponents.CircularProgressIndicator">
        <item name="indicatorSize">48dp</item>
        <item name="trackThickness">4dp</item>
        <item name="indicatorColor">@color/purple_500</item>
        <item name="trackColor">@android:color/darker_gray</item>
    </style>
</resources>